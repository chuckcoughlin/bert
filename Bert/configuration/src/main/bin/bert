#!/bin/sh
# Execute the robot control application standalone from the command line
# Clear logs first, then run in online mode per bert.xml
cd ${BERT_HOME}
bin/clear_logs
cd distribution/bertApp
export APP_HOME=`pwd`
cd bin
./bertApp ${BERT_HOME}